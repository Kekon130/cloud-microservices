apiVersion: v1
kind: Service
metadata:
  name: products-microservice-service
  namespace: cloud
spec:
  selector:
    app: products-microservice
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: app-port
