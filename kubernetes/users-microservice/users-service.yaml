apiVersion: v1
kind: Service
metadata:
  name: users-microservice-service
  namespace: cloud
spec:
  selector:
    app: users-microservice
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: app-port
