apiVersion: v1
kind: Service
metadata:
  name: orders-microservice-service
  namespace: cloud
spec:
  selector:
    app: orders-microservice
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: app-port
